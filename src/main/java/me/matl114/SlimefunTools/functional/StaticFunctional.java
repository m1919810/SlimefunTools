package me.matl114.SlimefunTools.functional;

public interface StaticFunctional {
    //should be like utils ,but have to register instances to run some function
    //these instances shouldn't renew when reload
}
